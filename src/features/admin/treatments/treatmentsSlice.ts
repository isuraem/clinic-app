// Redirected to products — use productsSlice instead
export { fetchProducts as fetchTreatments, saveProduct as saveTreatment, deleteProduct as deleteTreatment, selectProduct as selectTreatment, toggleProductActive as toggleTreatmentActive } from '@/features/admin/products/productsSlice'
export { default } from '@/features/admin/products/productsSlice'
